{
  "name": "larkbot-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "find src -name '*.mjs' -print0 | xargs -0 -n1 node --check",
    "test": "node --test --test-concurrency=1"
  }
}
